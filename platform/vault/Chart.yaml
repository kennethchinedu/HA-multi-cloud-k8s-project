apiVersion: v2
name: vault
description: HashiCorp Vault secrets management for the SRE cluster
type: application
version: 0.1.0
appVersion: "1.15.0"

dependencies:
  - name: vault
    version: "0.28.1"
    repository: "https://helm.releases.hashicorp.com"
